// Chess app entry. The whole app is a single screen (the game), so this file
// simply re-exports GameScreen as the default route component. Replaces the
// Expo template's home screen.

export { default } from '@/components/chess/GameScreen';
