[versions]
# kotlinx-coroutines + kermit are no longer declared here: they ship transitively with
# io.github.ber4444:chess-core (coroutines as `api`, kermit as `implementation`).
kotlin = "2.3.20"

[plugins]
kotlinMultiplatform = { id = "org.jetbrains.kotlin.multiplatform", version.ref = "kotlin" }
