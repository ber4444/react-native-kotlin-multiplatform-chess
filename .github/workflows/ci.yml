name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── chess-core: Kotlin/JS build + commonTest suite ───────────────────────────
  # Consumes io.github.ber4444:chess-core from GitHub Packages (published from
  # ber4444/compose-multiplatform-chess). GitHub Packages requires auth even for public packages,
  # so the build step needs GITHUB_ACTOR + a PAT with `read:packages` (repo secret CHESS_PACKAGES_TOKEN).
  chess-core:
    name: chess-core (Kotlin/JS + tests)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle
      - name: Build Kotlin/JS + run commonTest
        working-directory: chess-core
        run: ./gradlew jsNodeTest copyJsToApp --no-daemon
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_PACKAGES_TOKEN: ${{ secrets.CHESS_PACKAGES_TOKEN }}
      - name: Upload chess-core JS
        uses: actions/upload-artifact@v7
        with:
          name: chess-core-js
          path: my-app/src/generated/chess-core
          include-hidden-files: true

  # ── on-device-ai: smoke consumer of published onDeviceAi + coachApi ──────────
  # Proves io.github.ber4444:onDeviceAi + io.github.ber4444:coachApi resolve from GitHub Packages
  # and their public types compile on Kotlin/JS. Same auth as chess-core (PAT with read:packages).
  on-device-ai:
    name: on-device-ai (smoke consumer)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle
      - name: Checkout compose-multiplatform-chess (PR 86) for local publish
        uses: actions/checkout@v7
        with:
          repository: ber4444/compose-multiplatform-chess
          ref: publish-on-device-ai
          path: compose-multiplatform-chess
      - name: Publish onDeviceAi + coachApi to mavenLocal
        working-directory: compose-multiplatform-chess
        run: ./gradlew :onDeviceAi:publishToMavenLocal :coachApi:publishToMavenLocal --no-daemon
      - name: Resolve published artifacts + run smoke test
        working-directory: on-device-ai
        run: ./gradlew jsNodeTest --no-daemon
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_PACKAGES_TOKEN: ${{ secrets.CHESS_PACKAGES_TOKEN }}

  # ── RN app: typecheck + lint + web build ─────────────────────────────────────
  app-web:
    name: RN app (typecheck + lint + web)
    runs-on: ubuntu-latest
    needs: chess-core
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: my-app/package-lock.json
      - name: Install dependencies
        working-directory: my-app
        run: npm ci
      - name: Download chess-core JS
        uses: actions/download-artifact@v8
        with:
          name: chess-core-js
          path: my-app/src/generated/chess-core
      - name: Checkout compose-multiplatform-chess for assets
        uses: actions/checkout@v7
        with:
          repository: ber4444/compose-multiplatform-chess
          path: compose-multiplatform-chess
      - name: Generate 3D assets (split glb + piece paths)
        working-directory: my-app
        run: npm run prepare:assets
        env:
          COMPOSE_CHESS_PATH: ${{ github.workspace }}/compose-multiplatform-chess
      - name: Typecheck
        working-directory: my-app
        run: npm run typecheck
      - name: Lint
        working-directory: my-app
        run: npm run lint
      - name: Build web bundle
        working-directory: my-app
        run: npm run build:web

  # ── Electron desktop: syntax-check main/preload + electron binary ─────────────
  electron:
    name: Electron (scripts + electron check)
    runs-on: ubuntu-latest
    needs: app-web
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: my-app/package-lock.json
      - name: Install dependencies
        working-directory: my-app
        run: npm ci
      - name: Syntax-check Electron scripts
        working-directory: my-app
        run: node --check electron/main.cjs && node --check electron/preload.cjs
      - name: Verify electron binary
        working-directory: my-app
        run: npx electron --no-sandbox --version

  # ── iOS build (macOS runner) ──────────────────────────────────────────────────
  ios:
    name: iOS (xcodebuild, no signing)
    runs-on: macos-15
    needs: app-web
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: my-app/package-lock.json
      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest
      - name: Install Node dependencies
        working-directory: my-app
        run: npm ci
      - name: Download chess-core JS
        uses: actions/download-artifact@v8
        with:
          name: chess-core-js
          path: my-app/src/generated/chess-core
      - name: Checkout compose-multiplatform-chess for assets
        uses: actions/checkout@v7
        with:
          repository: ber4444/compose-multiplatform-chess
          path: compose-multiplatform-chess
      - name: Generate 3D assets
        working-directory: my-app
        run: npm run prepare:assets
        env:
          COMPOSE_CHESS_PATH: ${{ github.workspace }}/compose-multiplatform-chess
      - name: Cache CocoaPods
        id: pod-cache
        uses: actions/cache@v6
        with:
          path: my-app/ios/Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('my-app/ios/Podfile.lock') }}
          restore-keys: ${{ runner.os }}-pods-
      - name: CocoaPods install
        working-directory: my-app/ios
        run: pod install
      - name: xcodebuild (no signing)
        working-directory: my-app
        run: |
          xcodebuild \
            -workspace ios/myapp.xcworkspace \
            -scheme myapp \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            build

  # ── Android build ─────────────────────────────────────────────────────────────
  android:
    name: Android (gradlew assembleDebug)
    runs-on: ubuntu-latest
    needs: app-web
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: my-app/package-lock.json
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle
      - name: Install Node dependencies
        working-directory: my-app
        run: npm ci
      - name: Download chess-core JS
        uses: actions/download-artifact@v8
        with:
          name: chess-core-js
          path: my-app/src/generated/chess-core
      - name: Checkout compose-multiplatform-chess for assets
        uses: actions/checkout@v7
        with:
          repository: ber4444/compose-multiplatform-chess
          path: compose-multiplatform-chess
      - name: Generate 3D assets
        working-directory: my-app
        run: npm run prepare:assets
        env:
          COMPOSE_CHESS_PATH: ${{ github.workspace }}/compose-multiplatform-chess
      - name: assembleDebug
        working-directory: my-app/android
        run: ./gradlew assembleDebug --no-daemon -PreactNativeArchitectures=arm64-v8a
